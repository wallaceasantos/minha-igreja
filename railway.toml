[build]
builder = "railpack"
buildCommand = "npm install && npm run build && cp serve.json dist/serve.json"

[deploy]
startCommand = "cd dist && npx serve -l $PORT"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
