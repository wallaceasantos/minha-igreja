{
  "services": [
    {
      "name": "backend-api",
      "rootDirectory": "backend-nodejs",
      "port": 3000,
      "healthcheckPath": "/api/health"
    },
    {
      "name": "frontend",
      "rootDirectory": ".",
      "buildCommand": "npm install && npm run build",
      "startCommand": "npx serve dist -s -l $PORT"
    }
  ]
}
